
// this is a comment

BEGIN INPUT

    INTERVAL X(FLOAT);
    SCALAR Y(INTEGER);

END INPUT ;

BEGIN OUTPUT

    INTERVAL X(FLOAT);
    SCALAR Y(INTEGER);

END OUTPUT ;

BEGIN CONSTRAINT

 E1 : X = ABS(X,Y,Z) + 1/Y;

END CONSTRAINT;
